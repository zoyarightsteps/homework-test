openapi: 3.0.0
info:
  title: RightSteps Homework API
  version: 1.0.0
  description: |
    Homework module for the RightSteps platform.

    **Roles:**
    - `TUTOR` — create drafts, assign, grade, revert homework
    - `PARENT` — view child homework, save/submit answers on child's behalf, browse and purchase question bank access via cart
    - `ADMIN` — manage the question bank

    **Homework Lifecycle:**
    1. Tutor checks eligibility (`can-assign`)
    2. Tutor creates a `DRAFT` (`POST /homework`)
    3. Tutor adds questions (`POST /homework/{id}/questions`)
    4. Tutor assigns to child (`POST /homework/{id}/assign`) → `ASSIGNED`
    5. Parent saves child's answers (`draft`) → `IN_PROGRESS`
    6. Parent submits (`submit`) → `SUBMITTED`; MCQ auto-scored, written answers AI-graded async
    7. Tutor grades overall (`grade`) → `COMPLETED`
    8. Optionally tutor reverts questions (`revert`) → `REVERTED`; child resubmits (`resubmit`)

servers:
  - url: http://localhost:5000/v1
    description: Local development
  - url: https://api.rightsteps.app/v1
    description: Production

tags:
  - name: Homework — Tutor
    description: Create, assign, grade, and manage homework
  - name: Homework — Child
    description: Child homework (accessed via authenticated parent)
  - name: Homework — Parent
    description: Parent view of child homework and question bank pricing
  - name: Homework — Question Bank
    description: Browse published bank questions (active subscription required)
  - name: Homework — Parent Marketplace
    description: Parent marketplace listing, bundle detail, purchase, and cart flow for the question bank
  - name: Homework — Admin
    description: Admin endpoints for managing the question bank

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    AlsoBoughtBundle:
      type: object
      properties:
        bundleType:
          type: string
          enum: [SUBTOPIC, TOPIC, SUBJECT]
        entityId:
          type: string
          description: ID of the subtopic, topic, or subject
        name:
          type: string
          nullable: true
          description: Display name of the bundle
        buyerCount:
          type: integer
          description: Number of purchases used to derive this recommendation
        avgPricePence:
          type: integer
          description: Average price paid for this bundle in pence (GBP)

    MarketplaceBundleCard:
      type: object
      description: A question bank bundle card in the marketplace listing
      properties:
        bundleType:
          type: string
          enum: [SUBTOPIC, TOPIC, SUBJECT]
        entityId:
          type: string
          description: ID of the subtopic, topic, or subject
        name:
          type: string
          example: Linear Equations
        subjectName:
          type: string
          example: Mathematics
        subjectId:
          type: string
        yearGroupName:
          type: string
          description: Child's year group — derived from childId
          example: Year 7
        questionCount:
          type: integer
          example: 12
        pricePence:
          type: integer
          description: Total price before discount (pence)
          example: 1200
        discountPct:
          type: integer
          description: Discount percentage — SUBTOPIC 0%, TOPIC 10%, SUBJECT 20%
          example: 10
        discountPence:
          type: integer
          description: Discount amount in pence
          example: 120
        finalPricePence:
          type: integer
          description: Price after discount in pence
          example: 1080
        averageRating:
          type: number
          nullable: true
          description: Average rating rounded to 1 decimal place. Null if no reviews yet.
          example: 4.3
        totalReviews:
          type: integer
          example: 8
        buyerCount:
          type: integer
          description: Number of completed purchases for this bundle
          example: 23

    PreviewQuestion:
      type: object
      description: Condensed question shown in bundle detail preview (no answers)
      properties:
        id:
          type: string
        type:
          type: string
          enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        questionText:
          type: string
        difficulty:
          type: string
          nullable: true
          enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        examBoard:
          type: string
          nullable: true
          enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]

    BundleReview:
      type: object
      properties:
        id:
          type: string
        rating:
          type: integer
          minimum: 1
          maximum: 5
        reviewText:
          type: string
          nullable: true
        reviewerName:
          type: string
          example: Sarah
        childYearGroup:
          type: string
          example: Year 9
        createdAt:
          type: string
          format: date-time

    Pagination:
      type: object
      properties:
        total:
          type: integer
          example: 42
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 10
        hasNextPage:
          type: boolean
          example: true

    SubTopicFlat:
      type: object
      description: Flat subtopic item as returned in homework detail (order + subtopic fields merged)
      properties:
        order:
          type: integer
          example: 1
        id:
          type: string
          example: cmst1abc0001r4ujxiqej3no
        name:
          type: string
          example: Linear Equations
        code:
          type: string
          example: ALG-01

    HomeworkAnswer:
      type: object
      properties:
        id:
          type: string
        textAnswer:
          type: string
          nullable: true
        selectedOption:
          type: string
          nullable: true
        matchAnswer:
          type: object
          nullable: true
          additionalProperties:
            type: string
        orderAnswer:
          type: array
          items:
            type: string
        imageUploadIds:
          type: array
          items:
            type: string
        isDraft:
          type: boolean
        isSkipped:
          type: boolean
        isCorrect:
          type: boolean
          nullable: true
        aiGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        aiFeedback:
          type: string
          nullable: true
        tutorGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        tutorFeedback:
          type: string
          nullable: true
        submittedAt:
          type: string
          format: date-time
          nullable: true

    TutorHomeworkQuestion:
      type: object
      description: Question in tutor view — includes modelAnswer, correctOption, and objective details
      properties:
        id:
          type: string
        type:
          type: string
          enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        questionText:
          type: string
        modelAnswer:
          type: string
          nullable: true
        options:
          type: array
          items:
            type: string
        correctOption:
          type: string
          nullable: true
        matchPairs:
          type: object
          nullable: true
          additionalProperties:
            type: string
        orderItems:
          type: array
          items:
            type: string
        blankAnswers:
          type: array
          items:
            type: string
        passage:
          type: string
          nullable: true
        videoUrl:
          type: string
          nullable: true
        videoNote:
          type: string
          nullable: true
        imageUploadIds:
          type: array
          items:
            type: string
        examBoard:
          type: string
          nullable: true
          enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        level:
          type: string
          nullable: true
          enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        difficulty:
          type: string
          nullable: true
          enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        isReverted:
          type: boolean
        revertNote:
          type: string
          nullable: true
        order:
          type: integer
        isFromBank:
          type: boolean
        objectiveId:
          type: string
          nullable: true
          description: FK to CurriculumObjective (cuid)
        objectiveCode:
          type: string
          nullable: true
          description: Human-readable objective code e.g. ALG-01-1. Present when objectiveId is set.
          example: ALG-01-1
        learningObjective:
          type: string
          nullable: true
          description: Full objective text. Present when objectiveId is set.
        answers:
          type: array
          items:
            $ref: '#/components/schemas/HomeworkAnswer'

    ChildHomeworkQuestion:
      type: object
      description: Question in child/parent view — no modelAnswer or correctOption
      properties:
        id:
          type: string
        type:
          type: string
          enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        questionText:
          type: string
        passage:
          type: string
          nullable: true
        videoUrl:
          type: string
          nullable: true
        videoNote:
          type: string
          nullable: true
        imageUploadIds:
          type: array
          items:
            type: string
        options:
          type: array
          items:
            type: string
        matchPairs:
          type: object
          nullable: true
          additionalProperties:
            type: string
        orderItems:
          type: array
          items:
            type: string
        blankAnswers:
          type: array
          items:
            type: string
        examBoard:
          type: string
          nullable: true
          enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        level:
          type: string
          nullable: true
          enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        difficulty:
          type: string
          nullable: true
          enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        isReverted:
          type: boolean
        revertNote:
          type: string
          nullable: true
        order:
          type: integer
        answers:
          type: array
          items:
            $ref: '#/components/schemas/HomeworkAnswer'

    TutorHomeworkDetail:
      type: object
      properties:
        id:
          type: string
        status:
          type: string
          enum: [DRAFT, ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
        deadline:
          type: string
          format: date-time
          nullable: true
        instructions:
          type: string
          nullable: true
        estimatedMinutes:
          type: integer
          nullable: true
        score:
          type: number
          nullable: true
        scoreOutOf:
          type: number
          nullable: true
        aiGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        tutorGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        aiFeedback:
          type: string
          nullable: true
        tutorFeedback:
          type: string
          nullable: true
        parentNote:
          type: string
          nullable: true
        revertCount:
          type: integer
        submittedAt:
          type: string
          format: date-time
          nullable: true
        revertedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        cancelledAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        child:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
        subject:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            code:
              type: string
        subTopics:
          type: array
          items:
            $ref: '#/components/schemas/SubTopicFlat'
        questions:
          type: array
          items:
            $ref: '#/components/schemas/TutorHomeworkQuestion'

    ChildHomeworkDetail:
      type: object
      properties:
        id:
          type: string
        status:
          type: string
          enum: [ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
        deadline:
          type: string
          format: date-time
          nullable: true
        instructions:
          type: string
          nullable: true
        estimatedMinutes:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        submittedAt:
          type: string
          format: date-time
          nullable: true
        revertedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        aiGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        aiFeedback:
          type: string
          nullable: true
        tutorGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        tutorFeedback:
          type: string
          nullable: true
        score:
          type: number
          nullable: true
        scoreOutOf:
          type: number
          nullable: true
        revertCount:
          type: integer
        parentNote:
          type: string
          nullable: true
        subject:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            code:
              type: string
        topic:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            code:
              type: string
        subTopics:
          type: array
          items:
            $ref: '#/components/schemas/SubTopicFlat'
        questions:
          type: array
          items:
            $ref: '#/components/schemas/ChildHomeworkQuestion'

    HomeworkListItem:
      type: object
      description: Homework summary in tutor list
      properties:
        id:
          type: string
        status:
          type: string
          enum: [DRAFT, ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
        deadline:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        submittedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        cancelledAt:
          type: string
          format: date-time
          nullable: true
        estimatedMinutes:
          type: integer
          nullable: true
        score:
          type: number
          nullable: true
        scoreOutOf:
          type: number
          nullable: true
        aiGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        tutorGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        child:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
        subject:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            code:
              type: string
        _count:
          type: object
          properties:
            questions:
              type: integer
              example: 5

    ChildHomeworkListItem:
      type: object
      description: Homework summary in child/parent list
      properties:
        id:
          type: string
        status:
          type: string
          enum: [ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
        deadline:
          type: string
          format: date-time
          nullable: true
        instructions:
          type: string
          nullable: true
        estimatedMinutes:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        submittedAt:
          type: string
          format: date-time
          nullable: true
        revertedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        aiGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        tutorGrade:
          type: string
          nullable: true
          enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
        score:
          type: number
          nullable: true
        scoreOutOf:
          type: number
          nullable: true
        revertCount:
          type: integer
        subject:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            code:
              type: string
        _count:
          type: object
          properties:
            questions:
              type: integer

    BankQuestion:
      type: object
      description: Published bank question (browse view — no modelAnswer or correctOption)
      properties:
        id:
          type: string
        type:
          type: string
          enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        questionText:
          type: string
        examBoard:
          type: string
          nullable: true
          enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        level:
          type: string
          nullable: true
          enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        difficulty:
          type: string
          nullable: true
          enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        subTopicId:
          type: string
        objectiveId:
          type: string
          nullable: true
        options:
          type: array
          items:
            type: string
        orderItems:
          type: array
          items:
            type: string
        blankAnswers:
          type: array
          items:
            type: string
        matchPairs:
          type: object
          nullable: true
          additionalProperties:
            type: string
        imageUploadIds:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time

    AdminBankQuestion:
      type: object
      description: Bank question in admin view — includes all fields
      properties:
        id:
          type: string
        subjectId:
          type: string
        topicId:
          type: string
        subTopicId:
          type: string
        objectiveId:
          type: string
          nullable: true
        type:
          type: string
          enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        questionText:
          type: string
        modelAnswer:
          type: string
          nullable: true
        options:
          type: array
          items:
            type: string
        correctOption:
          type: string
          nullable: true
        matchPairs:
          type: object
          nullable: true
          additionalProperties:
            type: string
        orderItems:
          type: array
          items:
            type: string
        blankAnswers:
          type: array
          items:
            type: string
        imageUploadIds:
          type: array
          items:
            type: string
        examBoard:
          type: string
          nullable: true
          enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        level:
          type: string
          nullable: true
          enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        difficulty:
          type: string
          nullable: true
          enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        price:
          type: integer
          description: Price in pence
          example: 99
        isPublished:
          type: boolean
        isActive:
          type: boolean
        createdByAdminId:
          type: string
        createdAt:
          type: string
          format: date-time

    AnswerInput:
      type: object
      required: [questionId]
      properties:
        questionId:
          type: string
        textAnswer:
          type: string
          nullable: true
        selectedOption:
          type: string
          nullable: true
        matchAnswer:
          type: object
          nullable: true
          additionalProperties:
            type: string
        orderAnswer:
          type: array
          items:
            type: string
          default: []
        imageUploadIds:
          type: array
          items:
            type: string
          default: []
        isSkipped:
          type: boolean
          default: false

security:
  - BearerAuth: []

paths:

  /homework/tutor/sessions/{sessionId}/can-assign:
    get:
      tags: [Homework — Tutor]
      summary: Check if homework can be assigned for a session
      description: |
        Pre-flight check before building a draft. Verifies tutor eligibility, session completion, 24-hour assignment window, and session homework limit (max 5).

        Always returns `200`. `canAssign: false` is a business rejection, not an HTTP error.
      parameters:
        - in: path
          name: sessionId
          required: true
          schema:
            type: string
          example: cmq5rmgoz0001r4ujxiqej3no
      responses:
        '200':
          description: Check completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      canAssign:
                        type: boolean
                      reason:
                        type: string
                        description: Present only when canAssign is false
                      session:
                        type: object
                        description: Present only when canAssign is true
                        properties:
                          id:
                            type: string
                          childId:
                            type: string
                          subjectId:
                            type: string
                          endTime:
                            type: string
                            format: date-time
                      homeworkCount:
                        type: integer
                        description: Homeworks already linked to this session
                      remainingSlots:
                        type: integer
              examples:
                eligible:
                  summary: Can assign
                  value:
                    success: true
                    data:
                      canAssign: true
                      session:
                        id: cmq5rmgoz0001r4ujxiqej3no
                        childId: cmp1abc0001r4ujxiqej3no
                        subjectId: cmp2def0001r4ujxiqej3no
                        endTime: '2026-07-01T15:00:00.000Z'
                      homeworkCount: 1
                      remainingSlots: 4
                notEligible:
                  summary: Cannot assign
                  value:
                    success: true
                    data:
                      canAssign: false
                      reason: Homework must be assigned within 24 hours of the session
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor
        '404':
          description: Session not found

  /homework/tutor/curriculum:
    get:
      tags: [Homework — Tutor]
      summary: Browse curriculum for homework creation
      description: |
        Returns different shapes depending on query params:
        - **No params** → all year groups
        - **yearGroupId only** → subjects for that year group
        - **yearGroupId + subjectId** → full topic / subtopic / objective tree
      parameters:
        - in: query
          name: yearGroupId
          schema:
            type: string
          description: Required when subjectId is also provided
        - in: query
          name: subjectId
          schema:
            type: string
          description: Requires yearGroupId
      responses:
        '200':
          description: Curriculum data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
              examples:
                yearGroups:
                  summary: No params — year groups
                  value:
                    success: true
                    data:
                      yearGroups:
                        - id: cmyg1abc
                          code: Y7
                          name: Year 7
                          keyStage: KS3
                subjects:
                  summary: yearGroupId only — subjects
                  value:
                    success: true
                    data:
                      subjects:
                        - id: cmsub1abc
                          code: MATHS
                          name: Mathematics
                          type: CORE
                topicsTree:
                  summary: Both params — topics tree
                  value:
                    success: true
                    data:
                      subject:
                        id: cmsub1abc
                        code: MATHS
                        name: Mathematics
                        type: CORE
                      yearGroup:
                        id: cmyg1abc
                        code: Y7
                        name: Year 7
                        keyStage: KS3
                      topics:
                        - id: cmtopic1abc
                          name: Algebra
                          code: ALG
                          subTopics:
                            - id: cmst1abc
                              name: Linear Equations
                              code: ALG-01
                              objectives:
                                - id: cmobj1abc
                                  objectiveId: ALG-01-1
                                  learningObjective: Solve linear equations with one unknown
                                  sequenceNumber: 1
                                  difficulty: STANDARD
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or admin
        '404':
          description: No curriculum found for this subject and year group

  /homework:
    post:
      tags: [Homework — Tutor]
      summary: Create a homework draft
      description: |
        Creates a `DRAFT` homework. Not visible to children until assigned.
        Next step: `POST /homework/{homeworkId}/questions`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [subjectId, topicId]
              properties:
                subjectId:
                  type: string
                topicId:
                  type: string
                subTopicIds:
                  type: array
                  items:
                    type: string
                  description: Optional subtopic tags for the homework (min 1 if provided)
                instructions:
                  type: string
                  example: Show all working
                estimatedMinutes:
                  type: integer
                  minimum: 1
                  example: 30
            example:
              subjectId: cmsub1abc0001r4ujxiqej3no
              topicId: cmtopic1abc0001r4ujxiqej3no
              subTopicIds: [cmst1abc0001r4ujxiqej3no]
              instructions: Complete all questions
              estimatedMinutes: 30
      responses:
        '201':
          description: Draft created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        example: DRAFT
                      subjectId:
                        type: string
                      topicId:
                        type: string
                      instructions:
                        type: string
                        nullable: true
                      estimatedMinutes:
                        type: integer
                        nullable: true
                      createdAt:
                        type: string
                        format: date-time
        '400':
          description: Validation error or subject/topic not found
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor

  /homework/{homeworkId}/questions:
    post:
      tags: [Homework — Tutor]
      summary: Add questions to a draft
      description: |
        Appends questions to a `DRAFT` homework (can call multiple times, max 20 total).

        Each item is either:
        - **Bank question** — provide `bankQuestionId` + `order`. Content is copied from the bank. Requires active homework subscription.
        - **Custom question** — provide `type`, `questionText`, `order`, and type-specific fields.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [questions]
              properties:
                questions:
                  type: array
                  minItems: 1
                  maxItems: 10
                  items:
                    type: object
                    required: [order]
                    properties:
                      bankQuestionId:
                        type: string
                        description: Provide for bank questions; omit all other content fields
                      type:
                        type: string
                        enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
                        description: Required for custom questions
                      questionText:
                        type: string
                        description: Required for custom questions
                      modelAnswer:
                        type: string
                        nullable: true
                      passage:
                        type: string
                        nullable: true
                        description: For READING_COMPREHENSION
                      videoUploadId:
                        type: string
                        nullable: true
                      videoUrl:
                        type: string
                        format: uri
                        nullable: true
                      videoNote:
                        type: string
                        nullable: true
                      imageUploadIds:
                        type: array
                        items:
                          type: string
                        default: []
                      options:
                        type: array
                        items:
                          type: string
                        description: Required for MULTIPLE_CHOICE and TRUE_FALSE
                      correctOption:
                        type: string
                        nullable: true
                        description: Required for MULTIPLE_CHOICE and TRUE_FALSE
                      matchPairs:
                        type: object
                        nullable: true
                        additionalProperties:
                          type: string
                        description: Required for MATCH
                      orderItems:
                        type: array
                        items:
                          type: string
                        description: Required for ORDERING (min 2 items)
                      blankAnswers:
                        type: array
                        items:
                          type: string
                        description: Required for FILL_BLANK
                      examBoard:
                        type: string
                        nullable: true
                        enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
                      level:
                        type: string
                        nullable: true
                        enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
                      difficulty:
                        type: string
                        nullable: true
                        enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
                      order:
                        type: integer
                        minimum: 1
                      subTopicId:
                        type: string
                        nullable: true
                      objectiveId:
                        type: string
                        nullable: true
            examples:
              bankQuestion:
                summary: From question bank
                value:
                  questions:
                    - bankQuestionId: cmq5rmgoz0001r4ujxiqej3no
                      order: 1
              customMcq:
                summary: Custom MCQ
                value:
                  questions:
                    - type: MULTIPLE_CHOICE
                      questionText: What is the value of x in 2x = 10?
                      options: ['3', '4', '5', '6']
                      correctOption: '5'
                      order: 1
      responses:
        '201':
          description: Questions added
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      questions:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            type:
                              type: string
                            questionText:
                              type: string
                            order:
                              type: integer
                            isFromBank:
                              type: boolean
        '400':
          description: Homework not DRAFT, question limit exceeded, or validation error
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription required for bank questions
        '404':
          description: Homework or bank question not found

  /homework/{homeworkId}/assign:
    post:
      tags: [Homework — Tutor]
      summary: Assign draft to a child
      description: |
        Creates a new `ASSIGNED` homework record by copying the draft's questions and subtopics to a new record linked to the child.

        **Validations:**
        - Draft must have at least 1 question
        - Child must have had at least one session with this tutor
        - Deadline must be at least 24 hours from now
        - No overlapping subtopics assigned to this child in the last 7 days
        - If `sessionId` provided: session must be `COMPLETED` and within assignment window

        The original draft is unchanged and can be reassigned to other children.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [childId, deadline]
              properties:
                childId:
                  type: string
                sessionId:
                  type: string
                  nullable: true
                  description: Optional — link to a specific completed session
                deadline:
                  type: string
                  format: date-time
                  description: Must be at least 24 hours from now
                  example: '2026-07-10T23:59:59.000Z'
      responses:
        '201':
          description: Homework assigned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        example: ASSIGNED
                      childId:
                        type: string
                      sessionId:
                        type: string
                        nullable: true
                      deadline:
                        type: string
                        format: date-time
        '400':
          description: Deadline too soon, minimum questions not met, or session not completed
        '401':
          description: Unauthenticated
        '403':
          description: Child not associated with this tutor
        '404':
          description: Homework or child not found
        '409':
          description: Overlapping subtopics already assigned to child within last 7 days

  /homework/{homeworkId}/cancel:
    post:
      tags: [Homework — Tutor]
      summary: Cancel assigned or in-progress homework
      description: Marks the homework `CANCELLED`. Only works on `ASSIGNED` or `IN_PROGRESS` status.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Homework cancelled
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        example: CANCELLED
                      cancelledAt:
                        type: string
                        format: date-time
        '400':
          description: Homework is not ASSIGNED or IN_PROGRESS
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor
        '404':
          description: Homework not found

  /homework/{homeworkId}/delete-draft:
    post:
      tags: [Homework — Tutor]
      summary: Delete a draft homework
      description: Permanently deletes a `DRAFT` homework and all its questions. Only works on DRAFT status.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Draft deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      deleted:
                        type: boolean
                        example: true
        '400':
          description: Homework is not DRAFT
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor
        '404':
          description: Homework not found

  /homework/{homeworkId}/grade:
    post:
      tags: [Homework — Tutor]
      summary: Grade a submitted homework
      description: |
        Sets overall tutor grade and optional per-question grades. Transitions homework to `COMPLETED`.

        Requires active homework subscription. Only works on `SUBMITTED` status.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tutorGrade]
              properties:
                tutorGrade:
                  type: string
                  enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
                tutorFeedback:
                  type: string
                  nullable: true
                questionGrades:
                  type: array
                  default: []
                  items:
                    type: object
                    required: [questionId, tutorGrade]
                    properties:
                      questionId:
                        type: string
                      tutorGrade:
                        type: string
                        enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
                      tutorFeedback:
                        type: string
                        nullable: true
            example:
              tutorGrade: GOOD
              tutorFeedback: Great effort on algebra section
              questionGrades:
                - questionId: cmqst0001r4ujxiqej3no
                  tutorGrade: OUTSTANDING
                  tutorFeedback: Perfect working shown
      responses:
        '200':
          description: Homework graded and completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        example: COMPLETED
                      tutorGrade:
                        type: string
                        enum: [OUTSTANDING, GOOD, SATISFACTORY, NEEDS_IMPROVEMENT]
                      tutorFeedback:
                        type: string
                        nullable: true
                      completedAt:
                        type: string
                        format: date-time
        '400':
          description: Homework not SUBMITTED or invalid question IDs
        '401':
          description: Unauthenticated
        '403':
          description: Active homework subscription required
        '404':
          description: Homework not found

  /homework/{homeworkId}/revert:
    post:
      tags: [Homework — Tutor]
      summary: Revert specific questions for the child to redo
      description: |
        Marks questions `isReverted: true` with a note. Transitions homework to `REVERTED`. Child must resubmit only the reverted questions.

        Requires active homework subscription. Only works on `SUBMITTED` status.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [questions]
              properties:
                questions:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [questionId, revertNote]
                    properties:
                      questionId:
                        type: string
                      revertNote:
                        type: string
                        minLength: 1
                        example: Show your full working for this question.
            example:
              questions:
                - questionId: cmqst0001r4ujxiqej3no
                  revertNote: Your answer was incomplete — show your working.
      responses:
        '200':
          description: Questions reverted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        example: REVERTED
                      revertCount:
                        type: integer
                        example: 1
                      revertedAt:
                        type: string
                        format: date-time
        '400':
          description: Homework not SUBMITTED or invalid question IDs
        '401':
          description: Unauthenticated
        '403':
          description: Active homework subscription required
        '404':
          description: Homework not found

  /homework/tutor:
    get:
      tags: [Homework — Tutor]
      summary: List tutor's homeworks
      description: Paginated list of all homeworks created by this tutor (drafts and assigned). Filterable by status, subject, and child.
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
        - in: query
          name: status
          schema:
            type: string
            enum: [DRAFT, ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
        - in: query
          name: subjectId
          schema:
            type: string
        - in: query
          name: childId
          schema:
            type: string
      responses:
        '200':
          description: Paginated list of homeworks
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworks:
                        type: array
                        items:
                          $ref: '#/components/schemas/HomeworkListItem'
                      pagination:
                        $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor

  /homework/tutor/{homeworkId}:
    get:
      tags: [Homework — Tutor]
      summary: Get homework detail (tutor view)
      description: |
        Full homework detail with all questions (including modelAnswer, correctOption, objectiveCode, learningObjective) and all submitted answers with AI and tutor grades.

        `subTopics` is a flat array — each item has `order`, `id`, `name`, `code` directly (no nested `subTopic` wrapper).
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Homework detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/TutorHomeworkDetail'
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor
        '404':
          description: Homework not found

  /homework/child/{childId}:
    get:
      tags: [Homework — Child]
      summary: List homework assigned to a child
      description: Returns all non-draft homework for the child. Authenticated user must be the child's parent.
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
        - in: query
          name: status
          schema:
            type: string
            enum: [ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
      responses:
        '200':
          description: Paginated list of child's homeworks
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworks:
                        type: array
                        items:
                          $ref: '#/components/schemas/ChildHomeworkListItem'
                      pagination:
                        $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Child not found

  /homework/child/{childId}/{homeworkId}:
    get:
      tags: [Homework — Child]
      summary: Get homework detail (child view)
      description: |
        Full homework detail visible to the child — questions with the child's saved/submitted answers. Does NOT include modelAnswer or correctOption.

        `subTopics` is flat (same as tutor view).
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Homework detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/ChildHomeworkDetail'
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or child not belonging to parent
        '404':
          description: Homework not found

  /homework/child/{childId}/{homeworkId}/draft:
    post:
      tags: [Homework — Child]
      summary: Save draft answers
      description: |
        Saves answers as `isDraft: true`. Can be called multiple times. Uses upsert — re-saving a question overwrites the previous draft answer.

        Transitions `ASSIGNED` → `IN_PROGRESS` on first save.

        When homework is `REVERTED`, only reverted questions can be updated.
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [answers]
              properties:
                answers:
                  type: array
                  minItems: 1
                  items:
                    $ref: '#/components/schemas/AnswerInput'
      responses:
        '200':
          description: Draft answers saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworkId:
                        type: string
                      status:
                        type: string
                        example: IN_PROGRESS
                      savedCount:
                        type: integer
                        example: 3
        '400':
          description: Wrong status, question IDs don't belong to homework, or non-reverted question submitted during REVERTED status
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or child mismatch
        '404':
          description: Homework or child not found

  /homework/child/{childId}/{homeworkId}/submit:
    post:
      tags: [Homework — Child]
      summary: Submit homework
      description: |
        Finalises submission — marks all draft answers as submitted. Unanswered questions are auto-created as `isSkipped: true`.

        - MCQ / TRUE_FALSE: auto-scored immediately
        - Written answers: AI grading triggered asynchronously via `homework.submitted` event

        Only works on `ASSIGNED` or `IN_PROGRESS` status and before the deadline.
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Homework submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworkId:
                        type: string
                      status:
                        type: string
                        example: SUBMITTED
                      submittedAt:
                        type: string
                        format: date-time
                      totalQuestions:
                        type: integer
                      answeredCount:
                        type: integer
                      skippedCount:
                        type: integer
                      score:
                        type: number
                        nullable: true
                        description: MCQ score. Null if no MCQ/TRUE_FALSE questions.
                      scoreOutOf:
                        type: number
                        nullable: true
        '400':
          description: Already submitted, deadline passed, or wrong status
        '401':
          description: Unauthenticated
        '404':
          description: Homework or child not found

  /homework/child/{childId}/{homeworkId}/resubmit:
    post:
      tags: [Homework — Child]
      summary: Resubmit reverted questions
      description: |
        Only works when homework is `REVERTED`. All reverted questions must be included. Previous answers are archived to `HomeworkAnswerRevision` before being overwritten.

        MCQ questions are re-scored and AI grading is re-triggered for written answers.
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [answers]
              properties:
                answers:
                  type: array
                  minItems: 1
                  description: Must include all reverted questions — partial resubmission is not allowed
                  items:
                    $ref: '#/components/schemas/AnswerInput'
      responses:
        '200':
          description: Resubmission accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworkId:
                        type: string
                      status:
                        type: string
                        example: SUBMITTED
                      submittedAt:
                        type: string
                        format: date-time
                      resubmittedCount:
                        type: integer
                      score:
                        type: number
                        nullable: true
                      scoreOutOf:
                        type: number
                        nullable: true
        '400':
          description: Homework not REVERTED, non-reverted question included, or reverted question missing
        '401':
          description: Unauthenticated
        '404':
          description: Homework or child not found

  /homework/parent/marketplace/price:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Calculate question bank bundle price
      description: |
        Returns pricing for purchasing a subtopic, topic, or subject bundle.

        Discount tiers applied to the sum of individual question prices:
        - `SUBTOPIC` — 0%
        - `TOPIC` — 10%
        - `SUBJECT` — 20%

        All prices in pence (GBP).
      parameters:
        - in: query
          name: type
          required: true
          schema:
            type: string
            enum: [SUBTOPIC, TOPIC, SUBJECT]
        - in: query
          name: entityId
          required: true
          schema:
            type: string
          description: ID of the subtopic / topic / subject being priced
        - in: query
          name: yearGroupId
          required: true
          schema:
            type: string
          description: Required for all types to scope questions by year group
      responses:
        '200':
          description: Price calculated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      type:
                        type: string
                        enum: [SUBTOPIC, TOPIC, SUBJECT]
                      entityId:
                        type: string
                      questionCount:
                        type: integer
                        example: 12
                      totalPrice:
                        type: integer
                        description: Sum of all question prices in pence before discount
                        example: 1200
                      discountPct:
                        type: integer
                        description: Discount percentage
                        example: 10
                      discountAmount:
                        type: integer
                        description: Discount amount in pence
                        example: 120
                      finalPrice:
                        type: integer
                        description: Price after discount in pence
                        example: 1080
                      currency:
                        type: string
                        example: GBP
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent

  /homework/parent/marketplace/preview:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Preview a sample of questions from a bundle before purchase
      description: |
        Returns the **total question count** for the bundle and a **small sample**
        (up to 2 questions) so the parent can get a feel for the content before
        buying. Answers, options, and answer keys are not included.
        Use alongside `/homework/parent/marketplace/price` to show both price
        and a teaser before checkout.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: type
          required: true
          schema:
            type: string
            enum: [SUBTOPIC, TOPIC, SUBJECT]
        - in: query
          name: entityId
          required: true
          schema:
            type: string
          description: ID of the subtopic / topic / subject being previewed
        - in: query
          name: yearGroupId
          required: true
          schema:
            type: string
          description: Required for all types to scope questions by year group
      responses:
        '200':
          description: Preview retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      type:
                        type: string
                        enum: [SUBTOPIC, TOPIC, SUBJECT]
                      entityId:
                        type: string
                      questionCount:
                        type: integer
                        description: Total number of questions in the full bundle
                        example: 47
                      questions:
                        type: array
                        description: Sample of up to 2 questions — no answers or options included
                        maxItems: 2
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            type:
                              type: string
                              enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
                            questionText:
                              type: string
                            examBoard:
                              type: string
                              nullable: true
                            level:
                              type: string
                              nullable: true
                            difficulty:
                              type: string
                              nullable: true
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or no active subscription

  /homework/parent/marketplace/subjects:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Browse subjects with published questions (parent)
      description: Subjects that have at least one published bank question. Optionally filter by yearGroupId to show only subjects relevant to a child's year group.
      parameters:
        - in: query
          name: yearGroupId
          required: false
          schema:
            type: string
          description: Filter subjects to those with questions for this year group
        - in: query
          name: childId
          required: false
          schema:
            type: string
          description: Optional child ID (for reference, yearGroupId takes precedence for filtering)
      responses:
        '200':
          description: Subjects list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      subjects:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Mathematics
                            code:
                              type: string
                              example: MATHS
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent

  /homework/parent/marketplace/topics:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Browse topics for a subject and year group (parent)
      description: Topics (with nested subtopics) that have at least one published question for the given subject and year group.
      parameters:
        - in: query
          name: subjectId
          required: true
          schema:
            type: string
        - in: query
          name: yearGroupId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Topics with subtopics
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      topics:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Algebra
                            code:
                              type: string
                              example: ALG
                            subTopics:
                              type: array
                              items:
                                type: object
                                properties:
                                  id:
                                    type: string
                                  name:
                                    type: string
                                    example: Linear Equations
                                  code:
                                    type: string
                                    example: ALG-01
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: No curriculum found for this subject and year group

  /homework/parent/marketplace/subtopics:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Browse subtopics for a topic (parent)
      description: Subtopics for a topic that have at least one published bank question.
      parameters:
        - in: query
          name: topicId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Subtopics list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      subTopics:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Linear Equations
                            code:
                              type: string
                              example: ALG-01
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Topic not found

  /homework/parent/marketplace/checkout:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Create Stripe checkout for a single item
      description: |
        Single-item purchase flow. Creates a `QuestionBankPurchase` (PENDING) and a Stripe checkout session.

        After successful payment, the webhook at `POST /payment/webhook` (event `checkout.session.completed`) grants access automatically.

        Discount tiers:
        - `SUBTOPIC` — 0%
        - `TOPIC` — 10%
        - `SUBJECT` — 20% (requires child to have a `yearGroupId`)

        All prices in pence (GBP).

        **For multi-item purchases, use the cart checkout instead.**
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type, entityId, childId, successUrl, cancelUrl]
              properties:
                type:
                  type: string
                  enum: [SUBTOPIC, TOPIC, SUBJECT]
                entityId:
                  type: string
                  description: ID of the subtopic / topic / subject
                childId:
                  type: string
                  description: Child who will receive access
                yearGroupId:
                  type: string
                  description: Required when type is SUBJECT
                  nullable: true
                successUrl:
                  type: string
                  format: uri
                  example: https://app.rightsteps.app/success
                cancelUrl:
                  type: string
                  format: uri
                  example: https://app.rightsteps.app/cancel
            example:
              type: SUBTOPIC
              entityId: cmst1abc0001r4ujxiqej3no
              childId: cmp1child0001r4ujxiqej3no
              successUrl: https://app.rightsteps.app/success
              cancelUrl: https://app.rightsteps.app/cancel
      responses:
        '201':
          description: Checkout session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      checkoutUrl:
                        type: string
                        format: uri
                        description: Redirect parent to this Stripe-hosted checkout URL
                      sessionId:
                        type: string
                        description: Stripe checkout session ID
                      purchaseId:
                        type: string
                        description: QuestionBankPurchase record ID (PENDING until payment)
        '400':
          description: Content has no questions (price is 0), or validation error
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Child not found or not belonging to parent

  /homework/parent/marketplace/purchases:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Get purchase history
      description: Paginated list of completed question bank purchases made by this parent, ordered newest first. Only purchases with paymentStatus COMPLETED are returned.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Paginated purchase history
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      purchases:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            questionCount:
                              type: integer
                              example: 12
                            finalPricePence:
                              type: integer
                              example: 1080
                            currency:
                              type: string
                              example: GBP
                            paymentStatus:
                              type: string
                              enum: [COMPLETED]
                              description: Always COMPLETED — pending and failed purchases are excluded
                            purchasedAt:
                              type: string
                              format: date-time
                            accessGrantedAt:
                              type: string
                              format: date-time
                              nullable: true
                            child:
                              type: object
                              properties:
                                id:
                                  type: string
                                firstName:
                                  type: string
                                lastName:
                                  type: string
                            yearGroup:
                              type: object
                              properties:
                                id:
                                  type: string
                                code:
                                  type: string
                                  example: Y9
                                name:
                                  type: string
                                  example: Year 9
                            subTopic:
                              type: object
                              nullable: true
                              description: Populated when purchase type is SUBTOPIC, null otherwise
                              properties:
                                id:
                                  type: string
                                name:
                                  type: string
                            topic:
                              type: object
                              nullable: true
                              description: Populated when purchase type is TOPIC or SUBTOPIC, null otherwise
                              properties:
                                id:
                                  type: string
                                name:
                                  type: string
                            subject:
                              type: object
                              nullable: true
                              description: Populated for all purchase types
                              properties:
                                id:
                                  type: string
                                name:
                                  type: string
                      pagination:
                        type: object
                        properties:
                          total:
                            type: integer
                          page:
                            type: integer
                          limit:
                            type: integer
                          hasNextPage:
                            type: boolean
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or no active subscription

  /homework/parent/marketplace/reviews:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Get reviews for a bundle
      description: |
        Returns paginated reviews and aggregate rating for a question bank bundle.
        Only active reviews are returned. Includes averageRating and totalReviews for display on listing cards.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: bundleType
          required: true
          schema:
            type: string
            enum: [SUBTOPIC, TOPIC, SUBJECT]
        - in: query
          name: entityId
          required: true
          schema:
            type: string
          description: The id of the subtopic, topic, or subject being reviewed
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Reviews and aggregate rating
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      averageRating:
                        type: number
                        nullable: true
                        example: 4.3
                        description: Average star rating rounded to 1 decimal place. Null if no reviews yet.
                      totalReviews:
                        type: integer
                        example: 28
                        description: Total number of active reviews for this bundle
                      reviews:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            rating:
                              type: integer
                              minimum: 1
                              maximum: 5
                            reviewText:
                              type: string
                              nullable: true
                            reviewerName:
                              type: string
                              example: Sarah
                              description: First name only. Shown as Anonymous if review was deleted via GDPR request.
                            childYearGroup:
                              type: string
                              example: Year 9
                              description: Year group of the child at time of purchase
                            createdAt:
                              type: string
                              format: date-time
                      pagination:
                        type: object
                        properties:
                          total:
                            type: integer
                          page:
                            type: integer
                          limit:
                            type: integer
                          hasNextPage:
                            type: boolean
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or no active subscription

    post:
      tags: [Homework — Parent Marketplace]
      summary: Submit a review for a purchased bundle
      description: |
        Submit a star rating and optional written review for a question bank bundle.

        Rules:
        - Parent must have a completed purchase for this bundle
        - One active review per parent per bundle — returns 409 if already reviewed
        - If a parent deletes their review they can re-submit a new one
        - Review goes live immediately (no moderation queue)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bundleType, entityId, rating]
              properties:
                bundleType:
                  type: string
                  enum: [SUBTOPIC, TOPIC, SUBJECT]
                entityId:
                  type: string
                  description: The id of the subtopic, topic, or subject being reviewed
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                reviewText:
                  type: string
                  maxLength: 500
                  description: Optional written review
      responses:
        '201':
          description: Review submitted and live
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      review:
                        type: object
                        properties:
                          id:
                            type: string
                          rating:
                            type: integer
                          reviewText:
                            type: string
                            nullable: true
                          reviewerName:
                            type: string
                          childYearGroup:
                            type: string
                          createdAt:
                            type: string
                            format: date-time
        '403':
          description: No completed purchase found for this bundle
        '409':
          description: Parent has already reviewed this bundle

  /homework/parent/marketplace/reviews/edit:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Edit an existing review
      description: |
        Update rating and/or review text on a review the parent has already submitted.
        Updated review goes live immediately.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bundleType, entityId, rating]
              properties:
                bundleType:
                  type: string
                  enum: [SUBTOPIC, TOPIC, SUBJECT]
                entityId:
                  type: string
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                reviewText:
                  type: string
                  maxLength: 500
      responses:
        '200':
          description: Review updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      review:
                        type: object
                        properties:
                          id:
                            type: string
                          rating:
                            type: integer
                          reviewText:
                            type: string
                            nullable: true
                          reviewerName:
                            type: string
                          childYearGroup:
                            type: string
                          updatedAt:
                            type: string
                            format: date-time
        '404':
          description: Review not found

  /homework/parent/marketplace/reviews/delete:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Delete own review
      description: |
        Soft-deletes the parent's review for a bundle.

        On deletion:
        - Review is hidden from all public listings
        - Review text is cleared
        - Reviewer name is replaced with "Anonymous"
        - The star rating is no longer counted in the aggregate

        This endpoint satisfies GDPR deletion requests for review content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bundleType, entityId]
              properties:
                bundleType:
                  type: string
                  enum: [SUBTOPIC, TOPIC, SUBJECT]
                entityId:
                  type: string
      responses:
        '200':
          description: Review deleted
        '404':
          description: Review not found

  /homework/parent/marketplace/also-bought:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Get homework bundles related to a course or content item
      description: |
        Returns two lists of question bank bundles to display on a course/content detail page:

        **peopleBought** — Collaborative filtering. Bundles that other parents who purchased
        this same course/content also bought. May be empty if purchase data is sparse.

        **subjectBundles** — Subject-based recommendations. All bundles in the same subject(s)
        as the course/content, sorted by popularity (most purchased first). Always returns
        data as long as the course subject has a `subjectId` FK set.

        Both lists return at most `limit` items (default 6). Results are Redis-cached for 1 hour.

        **Note for courses:** `subjectBundles` only works when the `CourseSubject.subjectId`
        FK is set (via `POST /v1/courses/:courseId/subjects` with a `subjectId` field).
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: entityType
          required: true
          schema:
            type: string
            enum: [COURSE, CONTENT]
        - in: query
          name: entityId
          required: true
          schema:
            type: string
          description: ID of the course or content item
        - in: query
          name: limit
          schema:
            type: integer
            default: 6
            maximum: 20
          description: Max results in each list
      responses:
        '200':
          description: Bundle recommendations retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      peopleBought:
                        type: array
                        description: Bundles bought by other purchasers of this item (may be empty)
                        items:
                          $ref: '#/components/schemas/AlsoBoughtBundle'
                      subjectBundles:
                        type: array
                        description: Popular bundles in the same subject (always populated when subjectId is set)
                        items:
                          $ref: '#/components/schemas/AlsoBoughtBundle'
              example:
                success: true
                data:
                  peopleBought:
                    - bundleType: SUBTOPIC
                      entityId: cmpd83sou000v0llynqm9e86g
                      name: Cells and Organisation
                      buyerCount: 8
                      avgPricePence: 299
                  subjectBundles:
                    - bundleType: TOPIC
                      entityId: cmpd83sog000u0llygl6irfgi
                      name: Biology Fundamentals
                      buyerCount: 15
                      avgPricePence: 899
                    - bundleType: SUBJECT
                      entityId: cmpd83so4000t0llyo7nywpu1
                      name: Science
                      buyerCount: 22
                      avgPricePence: 1999
        '400':
          description: Invalid entityType or entityId
        '401':
          description: Unauthenticated

  /homework/parent/marketplace/bundles:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Browse marketplace bundles for a child
      description: |
        Returns a paginated, enriched flat listing of question bank bundles (SUBTOPIC, TOPIC, and/or SUBJECT)
        relevant to the child's year group. The year group is derived automatically from `childId`.

        Each card includes pricing (with discount), aggregated ratings, and buyer count.

        **Pricing tiers:**
        - `SUBTOPIC` — 0% discount
        - `TOPIC` — 10% discount
        - `SUBJECT` — 20% discount (questions scoped to child's year group)

        Results are Redis-cached per child and filter combination for 1 hour.
      parameters:
        - in: query
          name: childId
          required: true
          schema:
            type: string
          description: Child ID — used to derive year group and verify parent ownership
        - in: query
          name: bundleType
          schema:
            type: string
            enum: [SUBTOPIC, TOPIC, SUBJECT]
          description: Filter to a specific bundle type. Omit to return all three types.
        - in: query
          name: subjectId
          schema:
            type: string
          description: Filter to bundles in a specific subject
        - in: query
          name: sortBy
          schema:
            type: string
            enum: [popular, rating, price_low, price_high, name]
            default: popular
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
            maximum: 50
      responses:
        '200':
          description: Paginated marketplace bundles
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/MarketplaceBundleCard'
                      pagination:
                        type: object
                        properties:
                          total:
                            type: integer
                          page:
                            type: integer
                          limit:
                            type: integer
                          totalPages:
                            type: integer
              example:
                success: true
                data:
                  items:
                    - bundleType: SUBTOPIC
                      entityId: cmst1abc0001r4ujxiqej3no
                      name: Linear Equations
                      subjectName: Mathematics
                      subjectId: cmsub1abc0001r4ujxiqej3no
                      yearGroupName: Year 7
                      questionCount: 12
                      pricePence: 1200
                      discountPct: 0
                      discountPence: 0
                      finalPricePence: 1200
                      averageRating: 4.3
                      totalReviews: 8
                      buyerCount: 23
                    - bundleType: TOPIC
                      entityId: cmtopic1abc0001r4ujxiqej3no
                      name: Algebra
                      subjectName: Mathematics
                      subjectId: cmsub1abc0001r4ujxiqej3no
                      yearGroupName: Year 7
                      questionCount: 47
                      pricePence: 4700
                      discountPct: 10
                      discountPence: 470
                      finalPricePence: 4230
                      averageRating: null
                      totalReviews: 0
                      buyerCount: 15
                  pagination:
                    total: 34
                    page: 1
                    limit: 20
                    totalPages: 2
        '400':
          description: Child has no year group assigned
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent, no active subscription, or child does not belong to parent
        '404':
          description: Child not found

  /homework/parent/marketplace/bundles/{type}/{id}:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Get full detail for a single marketplace bundle
      description: |
        Returns complete detail for a SUBTOPIC, TOPIC, or SUBJECT bundle including:
        - Full entity info (name, description, code, parent topic/subject context)
        - Pricing with discount
        - Up to 5 recent reviews with aggregate rating
        - 2 preview questions (no answers/options)
        - Buyer count

        For TOPIC bundles, also returns a list of subtopics included.

        `childId` is required to scope SUBJECT-level pricing to the child's year group.
        Results are Redis-cached per bundle + child for 1 hour.
      parameters:
        - in: path
          name: type
          required: true
          schema:
            type: string
            enum: [SUBTOPIC, TOPIC, SUBJECT]
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: Entity ID (cuid) of the subtopic, topic, or subject
        - in: query
          name: childId
          required: true
          schema:
            type: string
          description: Child ID — used to scope SUBJECT pricing and verify parent ownership
      responses:
        '200':
          description: Bundle detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bundleType:
                        type: string
                        enum: [SUBTOPIC, TOPIC, SUBJECT]
                      entityId:
                        type: string
                      name:
                        type: string
                      description:
                        type: string
                        nullable: true
                      code:
                        type: string
                      subjectName:
                        type: string
                      subjectId:
                        type: string
                      yearGroupName:
                        type: string
                      topicName:
                        type: string
                        nullable: true
                        description: Present for SUBTOPIC bundles only
                      topicId:
                        type: string
                        nullable: true
                        description: Present for SUBTOPIC bundles only
                      subTopics:
                        type: array
                        nullable: true
                        description: Present for TOPIC bundles only — subtopics included in this bundle
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                            code:
                              type: string
                      questionCount:
                        type: integer
                        example: 12
                      pricePence:
                        type: integer
                        example: 1200
                      discountPct:
                        type: integer
                        example: 0
                      discountPence:
                        type: integer
                        example: 0
                      finalPricePence:
                        type: integer
                        example: 1200
                      currency:
                        type: string
                        example: GBP
                      buyerCount:
                        type: integer
                        example: 23
                      averageRating:
                        type: number
                        nullable: true
                        example: 4.3
                      totalReviews:
                        type: integer
                        example: 28
                      recentReviews:
                        type: array
                        description: Up to 5 most recent reviews
                        items:
                          $ref: '#/components/schemas/BundleReview'
                      previewQuestions:
                        type: array
                        description: Up to 2 preview questions (no answers)
                        items:
                          $ref: '#/components/schemas/PreviewQuestion'
              examples:
                subtopicDetail:
                  summary: SUBTOPIC bundle detail
                  value:
                    success: true
                    data:
                      bundleType: SUBTOPIC
                      entityId: cmst1abc0001r4ujxiqej3no
                      name: Linear Equations
                      description: Solving one-variable linear equations
                      code: ALG-01
                      subjectName: Mathematics
                      subjectId: cmsub1abc0001r4ujxiqej3no
                      yearGroupName: Year 7
                      topicName: Algebra
                      topicId: cmtopic1abc0001r4ujxiqej3no
                      questionCount: 12
                      pricePence: 1200
                      discountPct: 0
                      discountPence: 0
                      finalPricePence: 1200
                      currency: GBP
                      buyerCount: 23
                      averageRating: 4.3
                      totalReviews: 8
                      recentReviews:
                        - id: cmrev1abc
                          rating: 5
                          reviewText: Excellent variety of questions
                          reviewerName: Sarah
                          childYearGroup: Year 7
                          createdAt: '2026-06-15T10:00:00.000Z'
                      previewQuestions:
                        - id: cmq1abc
                          type: MULTIPLE_CHOICE
                          questionText: Solve for x when 2x + 3 = 11
                          difficulty: STANDARD
                          examBoard: AQA
        '400':
          description: Child has no year group assigned
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent, no active subscription, or child does not belong to parent
        '404':
          description: Bundle not found

  /homework/parent/marketplace/access:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Check child's access to a subtopic
      description: Returns whether the child has active access to a specific subtopic (i.e. a `QuestionBankAccess` record exists).
      parameters:
        - in: query
          name: childId
          required: true
          schema:
            type: string
        - in: query
          name: subTopicId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Access check result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      hasAccess:
                        type: boolean
                      grantedAt:
                        type: string
                        format: date-time
                        nullable: true
              examples:
                hasAccess:
                  value:
                    success: true
                    data:
                      hasAccess: true
                      grantedAt: '2026-07-01T10:30:00.000Z'
                noAccess:
                  value:
                    success: true
                    data:
                      hasAccess: false
                      grantedAt: null
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Child not found or not belonging to parent

  /homework/parent/marketplace/cart:
    get:
      tags: [Homework — Parent Marketplace]
      summary: Get cart contents
      description: |
        Returns the parent's current question bank cart with real-time pricing for each item.

        Each item includes:
        - `type` — `SUBTOPIC`, `TOPIC`, or `SUBJECT`
        - `label` — human-readable name of the entity
        - `pricing` — live pricing including discount and final price in pence
        - `child` — child who will receive access

        `total` is the sum of all `finalPrice` values.
      responses:
        '200':
          description: Cart contents
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: CartItem ID — use this in remove/checkout calls
                            entityType:
                              type: string
                              enum: [QBK_SUBTOPIC, QBK_TOPIC, QBK_SUBJECT]
                            entityId:
                              type: string
                            type:
                              type: string
                              enum: [SUBTOPIC, TOPIC, SUBJECT]
                            label:
                              type: string
                              example: Linear Equations
                            child:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: string
                                name:
                                  type: string
                                  example: Ayesha Khan
                                yearGroupId:
                                  type: string
                                  nullable: true
                            pricing:
                              type: object
                              properties:
                                questionCount:
                                  type: integer
                                  example: 12
                                totalPrice:
                                  type: integer
                                  description: Sum of question prices before discount (pence)
                                  example: 1200
                                discountPct:
                                  type: integer
                                  example: 0
                                discountAmount:
                                  type: integer
                                  example: 0
                                finalPrice:
                                  type: integer
                                  description: Price after discount (pence)
                                  example: 1200
                                currency:
                                  type: string
                                  example: GBP
                      total:
                        type: integer
                        description: Sum of all item finalPrice values in pence
                        example: 2400
                      currency:
                        type: string
                        example: GBP
              examples:
                cartWithItems:
                  summary: Cart with two items
                  value:
                    success: true
                    data:
                      items:
                        - id: cm_cartitem_001
                          entityType: QBK_SUBTOPIC
                          entityId: cmst1abc0001r4ujxiqej3no
                          type: SUBTOPIC
                          label: Linear Equations
                          child:
                            id: cm_child_001
                            name: Ayesha Khan
                            yearGroupId: cm_yg_001
                          pricing:
                            questionCount: 10
                            totalPrice: 1000
                            discountPct: 0
                            discountAmount: 0
                            finalPrice: 1000
                            currency: GBP
                        - id: cm_cartitem_002
                          entityType: QBK_TOPIC
                          entityId: cmtopic1abc0001r4ujxiqej3no
                          type: TOPIC
                          label: Algebra
                          child:
                            id: cm_child_001
                            name: Ayesha Khan
                            yearGroupId: cm_yg_001
                          pricing:
                            questionCount: 40
                            totalPrice: 4000
                            discountPct: 10
                            discountAmount: 400
                            finalPrice: 3600
                            currency: GBP
                      total: 4600
                      currency: GBP
                emptyCart:
                  summary: Empty cart
                  value:
                    success: true
                    data:
                      items: []
                      total: 0
                      currency: GBP
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent

  /homework/parent/marketplace/cart/items:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Add item to cart
      description: |
        Adds a single question bank item to the cart. Each combination of `(type, entityId, childId)` is unique — adding a duplicate returns `409`.

        **SUBJECT purchases require the child to have a `yearGroupId` assigned** — the service validates this at add time (400 if missing).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type, entityId, childId]
              properties:
                type:
                  type: string
                  enum: [SUBTOPIC, TOPIC, SUBJECT]
                entityId:
                  type: string
                  description: ID of the subtopic / topic / subject
                childId:
                  type: string
                  description: Child who will receive access upon purchase
            examples:
              addSubtopic:
                summary: Add a subtopic
                value:
                  type: SUBTOPIC
                  entityId: cmst1abc0001r4ujxiqej3no
                  childId: cmp1child0001r4ujxiqej3no
              addTopic:
                summary: Add a topic bundle
                value:
                  type: TOPIC
                  entityId: cmtopic1abc0001r4ujxiqej3no
                  childId: cmp1child0001r4ujxiqej3no
      responses:
        '201':
          description: Item added to cart
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      item:
                        type: object
                        properties:
                          id:
                            type: string
                            description: CartItem ID
                          entityType:
                            type: string
                            enum: [QBK_SUBTOPIC, QBK_TOPIC, QBK_SUBJECT]
                          entityId:
                            type: string
                          childId:
                            type: string
                          createdAt:
                            type: string
                            format: date-time
        '400':
          description: Child has no yearGroupId and type is SUBJECT
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent or child not belonging to parent
        '404':
          description: Child not found
        '409':
          description: Item already in cart (same type + entityId + childId combination)

  /homework/parent/marketplace/cart/items/remove:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Remove item from cart
      description: Removes a cart item by its ID. The `cartItemId` is the `id` returned from GET cart or POST cart/items.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [cartItemId]
              properties:
                cartItemId:
                  type: string
            example:
              cartItemId: cm_cartitem_001
      responses:
        '200':
          description: Item removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    nullable: true
                    example: null
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Cart item not found or does not belong to this user

  /homework/parent/marketplace/cart/checkout:
    post:
      tags: [Homework — Parent Marketplace]
      summary: Checkout all cart items via Stripe
      description: |
        Creates one Stripe checkout session covering all cart items as separate line items. Each item gets its own `QuestionBankPurchase` record (PENDING).

        After payment, the webhook auto-grants access for all items and clears the purchase IDs from Redis.

        **Pre-checkout validation:**
        - Cart must not be empty
        - All items must have `finalPrice > 0` (items with 0 price — no questions — are blocked)

        On success, redirect the parent to `checkoutUrl`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [successUrl, cancelUrl]
              properties:
                successUrl:
                  type: string
                  format: uri
                  example: https://app.rightsteps.app/success
                cancelUrl:
                  type: string
                  format: uri
                  example: https://app.rightsteps.app/cancel
      responses:
        '201':
          description: Checkout session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      checkoutUrl:
                        type: string
                        format: uri
                        description: Redirect parent to this Stripe-hosted checkout URL
                      sessionId:
                        type: string
                        description: Stripe checkout session ID
              examples:
                success:
                  value:
                    success: true
                    data:
                      checkoutUrl: https://checkout.stripe.com/pay/cs_test_abc123
                      sessionId: cs_test_abc123
        '400':
          description: Cart is empty, or one or more items have no available questions (price = 0)
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent

  /homework/parent/child/{childId}:
    get:
      tags: [Homework — Parent]
      summary: List homework for a child (parent view)
      description: Paginated list of all non-draft homework assigned to the child. Parent must own the child.
      parameters:
        - in: path
          name: childId
          required: true
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
        - in: query
          name: status
          schema:
            type: string
            enum: [ASSIGNED, IN_PROGRESS, SUBMITTED, REVERTED, COMPLETED, MISSED, CANCELLED]
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      homeworks:
                        type: array
                        items:
                          $ref: '#/components/schemas/ChildHomeworkListItem'
                      pagination:
                        $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Child not found

  /homework/parent/{homeworkId}:
    get:
      tags: [Homework — Parent]
      summary: Get homework detail (parent view)
      description: Full homework detail for a homework belonging to this parent. Same structure as child view including all answer grades.
      parameters:
        - in: path
          name: homeworkId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Homework detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/ChildHomeworkDetail'
        '401':
          description: Unauthenticated
        '403':
          description: Not a parent
        '404':
          description: Homework not found

  /homework/question-bank/subjects:
    get:
      tags: [Homework — Question Bank]
      summary: Browse subjects with published questions
      description: Subjects that have at least one published bank question. Requires active homework subscription.
      responses:
        '200':
          description: Subjects list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      subjects:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Mathematics
                            code:
                              type: string
                              example: MATHS
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription inactive

  /homework/question-bank/topics:
    get:
      tags: [Homework — Question Bank]
      summary: Browse topics with published questions
      description: Topics (with nested subtopics) for a given subject and year group that have at least one published question.
      parameters:
        - in: query
          name: subjectId
          required: true
          schema:
            type: string
        - in: query
          name: yearGroupId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Topics with subtopics
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      topics:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Algebra
                            code:
                              type: string
                              example: ALG
                            subTopics:
                              type: array
                              items:
                                type: object
                                properties:
                                  id:
                                    type: string
                                  name:
                                    type: string
                                    example: Linear Equations
                                  code:
                                    type: string
                                    example: ALG-01
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription inactive
        '404':
          description: No curriculum found for this subject and year group

  /homework/question-bank/subtopics:
    get:
      tags: [Homework — Question Bank]
      summary: Browse subtopics with published questions
      description: Subtopics for a topic that have at least one published bank question.
      parameters:
        - in: query
          name: topicId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Subtopics list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      subTopics:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                              example: Linear Equations
                            code:
                              type: string
                              example: ALG-01
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription inactive
        '404':
          description: Topic not found

  /homework/question-bank/questions:
    get:
      tags: [Homework — Question Bank]
      summary: Browse published bank questions
      description: Paginated list of published bank questions for a subtopic. Filter by type, examBoard, level, difficulty, and objective.
      parameters:
        - in: query
          name: subTopicId
          required: true
          schema:
            type: string
        - in: query
          name: objectiveId
          schema:
            type: string
        - in: query
          name: type
          schema:
            type: string
            enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        - in: query
          name: examBoard
          schema:
            type: string
            enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        - in: query
          name: level
          schema:
            type: string
            enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        - in: query
          name: difficulty
          schema:
            type: string
            enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Paginated bank questions
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      questions:
                        type: array
                        items:
                          $ref: '#/components/schemas/BankQuestion'
                      pagination:
                        $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription inactive

  /homework/question-bank/questions/{bankQuestionId}:
    get:
      tags: [Homework — Question Bank]
      summary: Get bank question detail
      description: Full detail of a single published bank question including modelAnswer and correctOption.
      parameters:
        - in: path
          name: bankQuestionId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Bank question detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      question:
                        allOf:
                          - $ref: '#/components/schemas/BankQuestion'
                          - type: object
                            properties:
                              modelAnswer:
                                type: string
                                nullable: true
                              correctOption:
                                type: string
                                nullable: true
                              topicId:
                                type: string
                              subjectId:
                                type: string
        '401':
          description: Unauthenticated
        '403':
          description: Not a tutor or subscription inactive
        '404':
          description: Bank question not found or not published

  /homework/admin/question-bank/questions:
    post:
      tags: [Homework — Admin]
      summary: Create a bank question
      description: |
        Creates a new bank question. Starts as unpublished (`isPublished: false`). Use the publish endpoint to make it visible to tutors.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [subjectId, topicId, subTopicId, type, questionText]
              properties:
                subjectId:
                  type: string
                topicId:
                  type: string
                subTopicId:
                  type: string
                objectiveId:
                  type: string
                  nullable: true
                type:
                  type: string
                  enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
                questionText:
                  type: string
                  minLength: 1
                modelAnswer:
                  type: string
                  nullable: true
                imageUploadIds:
                  type: array
                  items:
                    type: string
                  default: []
                options:
                  type: array
                  items:
                    type: string
                  description: Required for MULTIPLE_CHOICE and TRUE_FALSE
                correctOption:
                  type: string
                  nullable: true
                  description: Required for MULTIPLE_CHOICE and TRUE_FALSE
                matchPairs:
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                  description: Required for MATCH
                orderItems:
                  type: array
                  items:
                    type: string
                  description: Required for ORDERING (min 2 items)
                blankAnswers:
                  type: array
                  items:
                    type: string
                  description: Required for FILL_BLANK
                examBoard:
                  type: string
                  nullable: true
                  enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
                level:
                  type: string
                  nullable: true
                  enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
                difficulty:
                  type: string
                  nullable: true
                  enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
                price:
                  type: integer
                  minimum: 0
                  default: 0
                  description: Price in pence
      responses:
        '201':
          description: Bank question created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bankQuestion:
                        $ref: '#/components/schemas/AdminBankQuestion'
        '400':
          description: Validation error
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin
        '404':
          description: Subject, topic, subtopic, or objective not found

    get:
      tags: [Homework — Admin]
      summary: List all bank questions (admin)
      description: Paginated list of all bank questions including unpublished. Filterable by subject, topic, subtopic, objective, type, examBoard, level, difficulty, publish status, and keyword search.
      parameters:
        - in: query
          name: subjectId
          schema:
            type: string
        - in: query
          name: topicId
          schema:
            type: string
        - in: query
          name: subTopicId
          schema:
            type: string
        - in: query
          name: objectiveId
          schema:
            type: string
        - in: query
          name: type
          schema:
            type: string
            enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
        - in: query
          name: examBoard
          schema:
            type: string
            enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
        - in: query
          name: level
          schema:
            type: string
            enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
        - in: query
          name: difficulty
          schema:
            type: string
            enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
        - in: query
          name: isPublished
          schema:
            type: string
            enum: ['true', 'false']
        - in: query
          name: search
          schema:
            type: string
          description: Keyword search on questionText
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Paginated bank questions
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      questions:
                        type: array
                        items:
                          $ref: '#/components/schemas/AdminBankQuestion'
                      pagination:
                        $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin

  /homework/admin/question-bank/questions/{bankQuestionId}/update:
    post:
      tags: [Homework — Admin]
      summary: Update a bank question
      description: Partially updates a bank question. All fields optional — only provided fields are updated.
      parameters:
        - in: path
          name: bankQuestionId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subjectId:
                  type: string
                topicId:
                  type: string
                subTopicId:
                  type: string
                objectiveId:
                  type: string
                  nullable: true
                type:
                  type: string
                  enum: [MULTIPLE_CHOICE, SHORT_ANSWER, LONG_ANSWER, FILL_BLANK, MATCH, TRUE_FALSE, ORDERING, READING_COMPREHENSION, VIDEO]
                questionText:
                  type: string
                  minLength: 1
                modelAnswer:
                  type: string
                  nullable: true
                imageUploadIds:
                  type: array
                  items:
                    type: string
                options:
                  type: array
                  items:
                    type: string
                correctOption:
                  type: string
                  nullable: true
                matchPairs:
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                orderItems:
                  type: array
                  items:
                    type: string
                blankAnswers:
                  type: array
                  items:
                    type: string
                examBoard:
                  type: string
                  nullable: true
                  enum: [EDEXCEL, AQA, OCR, WJEC, CAMBRIDGE, IB, NONE]
                level:
                  type: string
                  nullable: true
                  enum: [KS3, GCSE, A_LEVEL, ELEVEN_PLUS, SATS]
                difficulty:
                  type: string
                  nullable: true
                  enum: [FOUNDATION, STANDARD, HIGHER, STRETCH]
                price:
                  type: integer
                  minimum: 0
                  description: Price in pence
      responses:
        '200':
          description: Bank question updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bankQuestion:
                        $ref: '#/components/schemas/AdminBankQuestion'
        '400':
          description: Validation error
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin
        '404':
          description: Bank question not found

  /homework/admin/question-bank/questions/{bankQuestionId}/publish:
    post:
      tags: [Homework — Admin]
      summary: Publish a bank question
      description: Marks the question as published. Makes it visible to tutors browsing the question bank.
      parameters:
        - in: path
          name: bankQuestionId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Bank question published
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bankQuestion:
                        $ref: '#/components/schemas/AdminBankQuestion'
        '400':
          description: Already published
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin
        '404':
          description: Bank question not found

  /homework/admin/question-bank/questions/{bankQuestionId}/unpublish:
    post:
      tags: [Homework — Admin]
      summary: Unpublish a bank question
      description: Marks the question as unpublished. Hides it from tutors without deleting.
      parameters:
        - in: path
          name: bankQuestionId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Bank question unpublished
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bankQuestion:
                        $ref: '#/components/schemas/AdminBankQuestion'
        '400':
          description: Already unpublished
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin
        '404':
          description: Bank question not found

  /homework/admin/question-bank/questions/{bankQuestionId}/deactivate:
    post:
      tags: [Homework — Admin]
      summary: Deactivate a bank question
      description: Soft deletes the bank question. Removes it from all browsing and pricing calculations.
      parameters:
        - in: path
          name: bankQuestionId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Bank question deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      bankQuestion:
                        $ref: '#/components/schemas/AdminBankQuestion'
        '400':
          description: Already deactivated
        '401':
          description: Unauthenticated
        '403':
          description: Not an admin
        '404':
          description: Bank question not found
